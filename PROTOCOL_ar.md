# PROTOCOL.md: نظام التطوير القائم على المهارات (Skill-Driven Development)

**Languages:** [English](PROTOCOL.md) · [Русский](PROTOCOL_ru.md) · **العربية** · [中文](PROTOCOL_zh.md) · [Deutsch](PROTOCOL_de.md) · [Español](PROTOCOL_es.md) · [Français](PROTOCOL_fr.md) · [日本語](PROTOCOL_ja.md) · [한국어](PROTOCOL_ko.md) · [Português](PROTOCOL_pt.md)

## 1. المفهوم والفلسفة

تصف هذه الوثيقة منهجية التطوير داخل المحفظة، والمكيفة لبيئة الوكيل الهجينة. يغطي البروتوكول دورة حياة المنتج بأكملها — من المسودة المعمارية الأولية إلى التوليد النهائي للقطع الأثرية التقديمية.

المبدأ الأساسي: **يجب أن تكون القرارات المعمارية صريحة وقابلة للتكرار والدفاع عنها.** لقد انتقلنا من مجرد كتابة التعليمات البرمجية إلى **التطوير القائم على المهارات (Skill-Driven Development)**، حيث يتم تفويض العمليات الروتينية والتصميم والاختبار والتحليلات إلى مهارات متخصصة لوكلاء محددين.

---

## 2. الأدوار وتوزيع المهارات

تشارك ثلاث كيانات رئيسية بالإضافة إلى بيئة الوكيل الموحدة في هذه العملية. أدوارهم منفصلة تمامًا ولا تتداخل.

### 2.1. المطور (بشري)
مالك المنتج. له الكلمة الأخيرة في كل نقطة قرار معماري، ويوافق على النطاق، ويحدد اتجاه التطوير، ويقبل مخرجات الوكلاء.

### 2.2. OpenCode (المنفذ المستقل)
الوكيل المنفذ، يعمل في الطرفية مع نافذة سياق تصل إلى 1 مليون رمز. مسؤول عن كتابة التعليمات البرمجية، وبناء الواجهات، وتوليد المستندات والقطع الأثرية الإعلامية.
يمتلك الترسانة التالية من المهارات:
*   **الهندسة والكود:** `code-review-skill`, `webapp-testing`, `mcp-builder`, `skill-creator`, `claude-api`.
*   **التصميم والواجهة الأمامية:** `frontend-design`, `web-artifacts-builder`, `theme-factory`, `canvas-design`, `algorithmic-art`, `brand-guidelines`.
*   **التوثيق والمكتب:** `build-project-docs`, `doc-coauthoring`, `docx`, `pdf`, `pptx`, `xlsx`.
*   **الاتصالات والتدريب:** `academy-guide`, `internal-comms`, `slack-gif-creator`, `discernment-nudge`.

### 2.3. Claude Desktop (المعماري والمحلل)
يعمل كمركز بيانات ومراجع معماري. لا يكتب كود الإنتاج مباشرة، ولكنه يتحقق من المنطق، ويحلل بيانات قاعدة البيانات، ويصيغ المهام لـ OpenCode.
ترسانة المهارات:
*   **إدارة السياق:** `morning`, `Import-memory`, `skill-creator`, `doc-coauthoring`.
*   **التحليلات والتحقق:** `analyze`, `data-context-extractor`, `explore-data`, `validate-data`, `statistical-analysis`.
*   **قاعدة البيانات وتصور البيانات:** `sql-queries`, `write-query`, `build-dashboard`, `create-viz`, `data-visualization`.

### 2.4. Antigravity (بيئة الوكيل الموحدة)
بيئة مستقلة تمامًا تدمج المجموعة الكاملة المكونة من 33 مهارة.
*   **القاعدة الأساسية:** يجب من الآن فصاعدًا إنشاء وصيانة جميع وثائق المشروع حصريًا من خلال Antigravity، مع الاستفادة من طرازي Gemini و Claude (كأفضل أدوات توثيق مع وصول غير مقيد إلى المهارات).

---

## 3. مراحل البروتوكول (دورة حياة المشروع)

### المرحلة 1: التهيئة و ARCHITECTURE.md
يتم صياغة الهندسة المعمارية قبل كتابة سطر واحد من الكود.
1.  يقوم **Claude Desktop** بتنشيط مهارات `morning` و `Import-memory` لتحميل السياق والعمل السابق. ثم يطبق `analyze` لتفكيك المتطلبات.
2.  يستخدم **OpenCode** مهارة `build-project-docs` لإنشاء مسودة لـ `ARCHITECTURE.md`.
3.  يتم تثبيت الوثيقة: هياكل البيانات، تنسيقات التخزين، المكدس التكنولوجي، وتقسيم الوحدات.

### المرحلة 2: Grill-me (اختبار إجهاد الهندسة المعمارية)
الهندسة المعمارية لا تؤخذ على محمل الثقة، بل يجب اختبار قوتها.
1.  يطبق **Claude Desktop** مهارة `data-context-extractor` لتحديد "النقاط العمياء" في البيانات و `doc-coauthoring` لتوليد أسئلة صعبة.
2.  قد يستخدم **OpenCode** مهارة `discernment-nudge` للتقييم الذاتي النقدي للحلول التقنية المقترحة.
3.  يتم إغلاق كل نقطة قرار خلافية بثلاثية: **الحل المختار -> سبب رفض البديل -> الاستثناءات من النطاق**.

### المرحلة 3: الانحرافات المتعمدة (Deliberate Deviations)
قسم في `ARCHITECTURE.md` حيث نسجل جميع الميزات والقدرات التي **نختار بوعي عدم بنائها**. إن حدود قدرات المشروع جزء لا يتجزأ من بنيته المعمارية. إذا تغير القرار أثناء التطوير، يتم نقل القرار القديم إلى هنا مع ذكر السبب.

### المرحلة 4: التنفيذ وحدة تلو الأخرى
يتقدم التطوير من أسفل إلى أعلى على طول رسم بياني للاعتمادية.
1.  يقوم **OpenCode** بتنفيذ جوهر المشروع. بالنسبة للتكاملات والبروتوكولات، يتم استخدام مهارات `mcp-builder` و `claude-api`.
2.  عند العمل على الجانب المرئي، يقوم **OpenCode** بتنشيط السلسلة: `brand-guidelines` -> `theme-factory` -> `frontend-design` -> `web-artifacts-builder`.
3.  لتوليد الرسومات الإجرائية أو اللوحات المعقدة، يتم تطبيق `algorithmic-art` و `canvas-design`.

### المرحلة 5: مراجعة الكود والاختبار
يتم دائمًا فصل التحقق من الصحة عن كتابة الكود.
1.  يقوم **OpenCode** بإجراء فحص منفصل باستخدام `code-review-skill` لتحديد الأخطاء والتنازلات.
2.  يتم إجراء اختبارات واجهة المستخدم والتكامل عبر مهارة `webapp-testing`. ويتم حفظ مخرجات الاختبار (stdout/stderr) دون تعديلات.
3.  يتدخل **Claude Desktop** للتحقق من معالجة البيانات: يستخدم `sql-queries` و `write-query` للتحقق من سلامة قاعدة البيانات، إلى جانب `validate-data` و `statistical-analysis` للتحقق من منطق العمل.

### المرحلة 6: توليد القطع الأثرية والتحليلات
يجب تقديم المشروع للمستخدم أو أصحاب المصلحة.
1.  يقوم **Claude Desktop**، باستخدام `build-dashboard` و `create-viz` و `data-visualization` بتشكيل تقارير بناءً на نتائج التطبيق أو المقاييس.
2.  يقوم **OpenCode** بتعبئة هذه البيانات في قطع أثرية تجارية جاهزة:
    *   التقارير والمواصفات: مهارات `pdf` و `docx` و `xlsx`.
    *   عروض الهندسة المعمارية: مهارة `pptx`.
    *   المواد التدريبية والداخلية: `academy-guide` و `internal-comms`.
    *   المحتوى الديناميكي للإعلانات: `slack-gif-creator`.

### المرحلة 7: قائمة التحقق النهائية
قبل الإصدار، يتم التحقق مما يلي:
*   مزامنة الكود النهائي مع `ARCHITECTURE.md`.
*   وجود سجلات اختبار فعلية.
*   غياب الملفات المؤقتة، وذاكرة التخزين المؤقت، والمفاتيح السرية.

---

## 4. سياسة اختيار النماذج (Model Selection Policy)

يعمل OpenCode على نماذج مجانية، ويتم تحديد اختيارها حسب المهمة:

| النموذج | الدور | الغرض | حالة الخصوصية |
| :--- | :--- | :--- | :--- |
| **Muse Spark 1.2 Free** | وكيل مستقل (أساسي) | تنفيذ مصفوفة المهارات الرئيسية، سياق 1 مليون رمز، منطق متعدد الخطوات في الطرفية. | فئة مجانية دائمة |
| **Nemotron 3 Ultra Free** | محلل عميق | الرياضيات الثقيلة، الخوارزميات المعقدة، إعادة هيكلة النظام على نطاق واسع. | **تجريبي من NVIDIA** — يتم تسجيل البيانات لتحسين المنتج. |
| **Nemotron 3.5 Lightning Free** | منفذ خلفي | التحقق السريع، استدعاءات الوظائف النفعية، معالجة خطوط الأنابيب الضخمة. | **تجريبي من NVIDIA** — نفس وضع Ultra. |
| **MiMo V2.5 Free** | مساعد واجهة المستخدم | تصحيح أخطاء لقطة الشاشة، تصميم واجهة المستخدم على الطاير (`frontend-design`). | فترة مجانية مؤقتة. |

بالنسبة لـ **Antigravity**، يتم استخدام **Gemini 3.5 Flash (Medium)** كمحرك أساسي لضمان الحد الأدنى من استهلاك الحدود/الحصص والسماح بالعمل المستمر على المهام والوثائق.

**قيود الأمان:** يُمنع **منعًا باتًا** تمرير المفاتيح الخاصة، والرموز، وقواعد البيانات الحقيقية، والمستودعات الخاصة إلى نقاط النهاية التجريبية (Nemotron, MiMo). يتم استخدام بيئة محلية أو موثوقة فقط للبيانات الحساسة.

---

## 5. مبادئ النظام البيئي الأساسية

1. **القرار الصريح أفضل من الافتراضي المريح.** إذا واجه الوكيل مفترق طرق، فإنه لا يخمن؛ بل يحدد الخيارات وينتظر الموافقة (أو يسجل التنازل).
2. **تُستخدم المهارات للغرض المقصود منها.** لا داعي لتوليد جداول Markdown إذا كان تقرير Excel مطلوبًا (استخدم `xlsx`). لا داعي لوصف لوحة معلومات بنص (استخدم `build-dashboard` + `create-viz`).
3. **الخطأ الذي يتم اكتشافه في المراجعة يعني نظامًا يعمل.** إن العثور على خطأ أثناء مرحلة المراجعة عبر `code-review-skill` هو دليل على أن الفلتر ثنائي المراحل يعمل.
4. **حدود المشروع مصونة.** الأداة نصف المكتملة التي تقوم بكل شيء أسوأ من أداة متخصصة للغاية مع قسم انحرافات متعمدة (Deliberate Deviations) موثق بوضوح.
