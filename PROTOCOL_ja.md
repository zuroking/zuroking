# PROTOCOL.md: スキル駆動型開発（Skill-Driven Development）エコシステム

**Languages:** [English](PROTOCOL.md) · [Русский](PROTOCOL_ru.md) · [العربية](PROTOCOL_ar.md) · [中文](PROTOCOL_zh.md) · [Deutsch](PROTOCOL_de.md) · [Español](PROTOCOL_es.md) · [Français](PROTOCOL_fr.md) · **日本語** · [한국어](PROTOCOL_ko.md) · [Português](PROTOCOL_pt.md)

## 1. コンセプトと哲学

本書は、ハイブリッドエージェントエコシステムに適応した、ポートフォリオ内の開発手法について説明します。本プロトコルは、初期のアーキテクチャ設計から最終的なプレゼンテーションアーティファクトの生成に至るまで、製品のライフサイクル全体をカバーします。

基本原則：**アーキテクチャ上の意思決定は、明示的で、再現可能で、説明可能でなければならない。** 私たちは単にコードを書くことから、日常業務、設計、テスト、分析を特定のエージェントの専門スキルに委任する**スキル駆動型開発（Skill-Driven Development）**へと移行しました。

---

## 2. 役割とスキル分配

このプロセスには、3つの主要なエンティティと統合エージェント環境が参加します。それらの役割は厳密に分離されており、重複することはありません。

### 2.1. 開発者（人間）
プロダクトオーナー。すべてのアーキテクチャ上の意思決定ポイントにおいて最終決定権を持ち、スコープを承認し、開発の方向性を設定し、エージェントの成果物を受け入れます。

### 2.2. OpenCode（自律的実行者）
最大1Mトークンのコンテキストウィンドウを持ち、ターミナルで動作する実行エージェント。コードの記述、インターフェースの構築、文書およびメディアアーティファクトの生成を担当します。
以下のスキル群を保有しています：
*   **エンジニアリングおよびコード：** `code-review-skill`, `webapp-testing`, `mcp-builder`, `skill-creator`, `claude-api`。
*   **デザインおよびフロントエンド：** `frontend-design`, `web-artifacts-builder`, `theme-factory`, `canvas-design`, `algorithmic-art`, `brand-guidelines`。
*   **ドキュメントおよびオフィス：** `build-project-docs`, `doc-coauthoring`, `docx`, `pdf`, `pptx`, `xlsx`。
*   **コミュニケーションおよびトレーニング：** `academy-guide`, `internal-comms`, `slack-gif-creator`, `discernment-nudge`。

### 2.3. Claude Desktop（アーキテクトおよびアナリスト）
データセンターおよびアーキテクチャレビューアとして機能します。本番環境のコードを直接書くことはありませんが、ロジックを検証し、データベースのデータを分析し、OpenCodeのためのタスクを作成します。
スキル群：
*   **コンテキスト管理：** `morning`, `Import-memory`, `skill-creator`, `doc-coauthoring`。
*   **分析および検証：** `analyze`, `data-context-extractor`, `explore-data`, `validate-data`, `statistical-analysis`。
*   **DBおよびデータ視覚化：** `sql-queries`, `write-query`, `build-dashboard`, `create-viz`, `data-visualization`。

### 2.4. Antigravity（統合エージェント環境）
33個の全スキルを統合した、完全に自律的な環境。
*   **重要ルール：** 今後、すべてのプロジェクトドキュメントは、スキルへの無制限のアクセス権を持つ主要なドキュメント作成ツールとして、GeminiおよびClaudeモデルを活用し、Antigravityを通じてのみ作成および維持されなければならない。

---

## 3. プロトコルの段階（プロジェクトのライフサイクル）

### 段階 1：初期化と ARCHITECTURE.md
コードが1行も書かれる前に、アーキテクチャが策定されます。
1.  **Claude Desktop** は `morning` および `Import-memory` スキルを有効にして、コンテキストと以前の作業をロードします。その後、`analyze` を適用して要件を分解します。
2.  **OpenCode** は `build-project-docs` を使用して `ARCHITECTURE.md` のドラフトを作成します。
3.  文書の確定：データ構造、保存フォーマット、技術スタック、およびモジュールの構成。

### 段階 2：Grill-me（アーキテクチャのストレステスト）
アーキテクチャは信用のみで受け入れられるものではありません。批判的に検証される必要があります。
1.  **Claude Desktop** は `data-context-extractor` を適用してデータ内の「ブラインドスポット」を特定し、`doc-coauthoring` を使用して答えにくい質問を生成します。
2.  **OpenCode** は、提案された技術的解決策の批判的な自己評価のために `discernment-nudge` を使用する場合があります。
3.  すべての論争となる意思決定ポイントは、次の3要素で解決されます：**選択された解決策 -> 代替案を却下した理由 -> スコープからの除外事項**。

### 段階 3：意図的な逸脱（Deliberate Deviations）
`ARCHITECTURE.md` 内のセクションで、私たちが**意識的に構築しないことを選択した**すべての機能と能力を記録します。プロジェクトの能力の境界は、そのアーキテクチャの完全な一部です。開発中に決定が変更された場合、古い決定はその理由とともにここに移動されます。

### 段階 4：モジュールごとの実装
開発は依存関係グラフに沿ってボトムアップで進められます。
1.  **OpenCode** はプロジェクトのコアを実装します。統合とプロトコルには、`mcp-builder` と `claude-api` が利用されます。
2.  ビジュアル面に取り組む際、**OpenCode** は次のチェーンを起動します：`brand-guidelines` -> `theme-factory` -> `frontend-design` -> `web-artifacts-builder`。
3.  手続き的なグラフィックス生成や複雑なキャンバスには、`algorithmic-art` と `canvas-design` が適用されます。

### 段階 5：コードレビューとテスト
検証は常にコードの記述と切り離されます。
1.  **OpenCode** は `code-review-skill` を使用して個別のパスを実行し、バグや妥協点を特定します。
2.  UIおよび統合テストは `webapp-testing` スキルを介して実施されます。テスト出力 (stdout/stderr) は変更せずに保存されます。
3.  **Claude Desktop** はデータ処理の検証に関与します：`sql-queries` と `write-query` を使用してデータベースの整合性をチェックし、`validate-data` と `statistical-analysis` を使用してビジネスロジックを検証します。

### 段階 6：アーティファクトと分析の生成
プロジェクトはユーザーまたは利害関係者に提示される必要があります。
1.  **Claude Desktop** は、`build-dashboard`、`create-viz`、および `data-visualization` を使用して、アプリケーションの結果またはメトリクスに基づいたレポートを作成します。
2.  **OpenCode** は、これらのデータをすぐに使用できるビジネスアーティファクトにパッケージ化します：
    *   レポートと仕様書：`pdf`、`docx`、`xlsx` スキル。
    *   アーキテクチャプレゼンテーション：`pptx` スキル。
    *   トレーニングおよび内部資料：`academy-guide`、`internal-comms`。
    *   アナウンス用の動的コンテンツ：`slack-gif-creator`。

### 段階 7：最終チェックリスト
リリース前に、以下が検証されます：
*   最終コードと `ARCHITECTURE.md` の同期。
*   実際のテストログの存在。
*   一時ファイル、キャッシュ、およびシークレットキーの不在。

---

## 4. モデル選択ポリシー（Model Selection Policy）

OpenCodeは無料モデルで動作し、その選択はタスクによって決定されます：

| モデル | 役割 | 目的 | プライバシーステータス |
| :--- | :--- | :--- | :--- |
| **Muse Spark 1.2 Free** | 自律的エージェント（コア） | メインスキルマトリクスの実行、1Mトークンコンテキスト、ターミナルでのマルチステップロジック。 | 恒久的な無料枠 |
| **Nemotron 3 Ultra Free** | ディープアナリスト | 高度な数学、複雑なアルゴリズム、大規模なシステムリファクタリング。 | **NVIDIAトライアル** — 製品向上のためデータがログに記録されます。 |
| **Nemotron 3.5 Lightning Free** | バックグラウンド実行者 | 迅速な検証、実用的な関数呼び出し、大量のパイプライン処理。 | **NVIDIAトライアル** — Ultraと同様。 |
| **MiMo V2.5 Free** | UI/UXアシスタント | スクリーンショットデバッグ、その場での `frontend-design`。 | 一時的な無料期間。 |

**Antigravity** では、制限やクォータの消費を最小限に抑え、タスクとドキュメント作成の継続的な作業を可能にするため、**Gemini 3.5 Flash (Medium)** がプライマリエンジンとして使用されます。

**セキュリティ制限：** 試用エンドポイント（Nemotron、MiMo）にプライベートキー、トークン、実際のデータベース、およびプライベートリポジトリを渡すことは**厳しく禁止されています**。機密データには、ローカル環境または信頼できる環境のみが使用されます。

---

## 5. エコシステムの基本原則

1. **明示的な決定は、便利なデフォルトよりも優れている。** エージェントが岐路に立たされたとき、推測するのではなく、選択肢を提示して承認を待ちます（または妥協点を記録します）。
2. **スキルは意図された目的に使用される。** Excelレポートが必要な場合にMarkdownテーブルを生成する必要はありません（`xlsx`を使用）。ダッシュボードをテキストで記述する必要はありません（`build-dashboard` + `create-viz`を使用）。
3. **レビューでバグが見つかることは、システムが機能していることを意味する。** `code-review-skill` を通じてレビュー段階でバグが発見されることは、2段階のフィルターが機能していることの証明です。
4. **プロジェクトの境界は侵すことができない。** 作りかけの「何でもできる」ツールは、明確に文書化された「意図的な逸脱（Deliberate Deviations）」セクションを持つ高度に専門化されたツールよりも劣ります。
